language: python
cache: pip
python:
  - "3.5"
notifications:
  email:
    - ryan@stickystyle.net
install: "pip install homeassistant"
before_script: >-
  find $PWD -name "*.yaml" | xargs grep "\!secret"| awk '{print $4": 25"}' > ./secrets.yaml
script:
  - hass --script check_config -c .